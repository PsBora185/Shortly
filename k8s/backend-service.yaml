apiVersion: v1
kind: Service
metadata:
  name: shortly-backend
  namespace: shortly
spec:
  selector:
    app: shortly-backend
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30081
  type: NodePort
